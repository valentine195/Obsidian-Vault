```dataview
table rarity as Rarity, length(rows) as "Count" from "Compendium/Items"
where tier = "minor"

group by rarity

```

```dataview
table rarity as Rarity, length(rows) as "Count" from "Compendium/Items"
where tier = "major" and includes(tags, "item/given")

group by rarity

```